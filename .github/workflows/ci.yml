name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  intellij-nomo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - name: Test
        run: ./gradlew test --no-daemon

      - name: Build plugin
        run: ./gradlew buildPlugin --no-daemon

      - name: Verify plugin configuration and compatibility
        run: ./gradlew verifyPluginProjectConfiguration verifyPlugin --no-daemon
